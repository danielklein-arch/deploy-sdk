{
  "name": "@danielklein-arch/deploy-sdk",
  "version": "0.0.0",
  "type": "module",
  "description": "Wrangler-only Cloudflare Workers deploy engine — per-PR previews + stable envs (dev/staging/prod). Bun-only.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && bun build ./src/index.ts --outdir dist --target bun && tsc",
    "typecheck": "tsc --noEmit",
    "prepare": "bun run build"
  },
  "peerDependencies": {
    "wrangler": ">=4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0",
    "wrangler": "4.98.0"
  }
}
